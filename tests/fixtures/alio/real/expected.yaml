# real/ 에 넣은 파일별 기대값. 파일마다 한 항목. 비어 있으면 실제 파일 테스트는 skip 된다.
files: []
# - file: korail_2024_track.pdf
#   fmt: pdf                     # pdf | hwpx
#   sha256: <shasum -a 256 결과>
#   pages: 120                   # hwpx 는 생략
#   title_contains: 궤도          # 메타데이터 제목 또는 본문 첫 줄에 들어있어야 할 문자열
#   text_contains: [제1장, 결론]
#   toc_min: 5                   # 목차 항목 최소 개수 (hwpx 는 0)
#   min_chars: 10000             # 추출 본문 최소 길이
